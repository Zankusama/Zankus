# completeness.md — 类别完整性清单（充分性审查）

> **定位**：评估器判「必要条件」（有没有），判不了「充分性」（全不全）。
> 本清单按 skill 类别穷尽列出「这类 skill 该有的」，AI 逐项核对，**不许「评估器没点名就跳过」**。
> 诊断与复查都必走：**评估器未过项 + 本清单缺口 = 处方**（双源合并）。

## 一、怎么用

1. **分类**：按 SKILL.md description/正文特征词判断类别（见「三、分类规则」）
2. **先过通用骨架**：所有类别先核对「二、通用六零件骨架」
3. **再过类别清单**：按类别过「四、各类别清单」
4. **每项结论四选一**：
   - `[脚本化✓]` — 已固化为脚本（SKILL.md 有声明 + 脚本文件真实存在）
   - `[命令化✓]` — 有明确可执行命令（可复制运行，退出码可判）
   - `[显式留AI✓]` — 明确写「由 AI 判断」且给理由（如语义判断无法机器化）
   - `[❌缺口]` — 该有但没有任何处理 → **进处方**
5. **两源合并**：评估器未过项 + 本清单缺口，全进治疗

## 二、通用六零件骨架（所有类别必核）

skill 工作流骨架 = 六类可写死的零件。逐项核对「有没有写死」：

| # | 零件 | 是什么 | WorkBuddy 落法 | 核对结论 |
|:--:|:---|:---|:---|:---|
| 1 | 触发 | 什么时候跑：定时 / 事件 / 手动 | 定时=automation｜事件=hook｜手动=SKILL.md 触发词 | ☐ |
| 2 | 步骤 | 什么顺序：顺序 / 分支 / 检查点 | SKILL.md 流程 + 产出物锁/闸门脚本 | ☐ |
| 3 | 材料 | 用什么料：固定输入 / 模板 | references/ 数据源 + 模板文件 | ☐ |
| 4 | 规则 | 边界+质检：权限 / 验收标准 | SKILL.md 死规矩 + 评估器 + 本清单 | ☐ |
| 5 | 工具 | 用什么手：固定连接器 / 脚本 | scripts/ + MCP 连接器 | ☐ |
| 6 | 输出 | 成品什么样：格式 / 位置 | 输出模板 + 保存路径规则 | ☐ |

**hook 事件层核对**（触发零件的强制层）：

- skill 骨架三层：SKILL.md=引导层（AI 自觉）→ scripts/=工具层（AI 调用）→ hooks/=强制层（系统自动，物理拦截）。hook 是最后防线，三层配合才完整。
- **挂 hook 判定**：某操作满足 ①违规代价高（改核心文件/发布/删数据）②AI 靠自觉容易漏（跳步/忘跑前置）→ 应挂 hook 物理拦截。
- 满足条件但无 hook → 标 `[❌事件层缺口]` 进处方；已有脚本强制的地方不必重复挂（避免冗余）。

**失败优先设计核对**（生命周期层）：

- 行业做法（评测驱动 6 步法）：**先写"已知失败场景"的评测用例，再设计 skill**——评测是设计前提，不是事后判分。
- 核对项：①测试集（goldens/eval_set）是否覆盖真实踩过的失败场景（而非只测 happy path）？②迭代时是否"先跑回归再改"（失败优先）？
- 达标：有失败场景样本 + 迭代先回归；缺口 → 标 `[❌失败优先缺口]` 进处方。

**反模式检查核对**（生命周期层）：

- 常见反模式（行业清单裁剪）：①触发词太泛（什么都触发）②输出无格式声明（产物随心所欲）③规则堆砌（无分级全硬指令）④依赖不声明（用 python3/node 但没写依赖）⑤无边界（不接的没排除）⑥重复工作没脚本化（手敲可机器化操作）。
- 核对项：该 skill 是否命中任一反模式？命中 → 标 `[❌反模式:xxx]` 进处方。

**真实路径校准核对**（生命周期层）：

- 行业做法：实际使用后的**误触发/漏步骤/产物偏差**反馈进测试集（评测驱动闭环最后一步）——不是"测过就行"，是"用坏了就补用例"。
- 核对项：①该 skill 有没有"使用反馈→补测试用例"的路径说明？②诊断报告是否记录真实使用暴露的问题（而非只记静态发现）？
- 达标：有反馈路径 + 诊断报告含真实使用证据；缺口 → 标 `[❌校准缺口]` 进处方。

## 三、分类规则（按 SKILL.md 特征词）

| 类别 | 特征词（description/正文） |
|:---|:---|
| 知识库编译类 | 知识库 / wiki / 知识编译 / 资料库 / 索引 / manifest / 缓存 |
| 工作流自动化类 | 循环执行 / 闭环 / 阶段 / 多轮 / 后台跑 / 自主跑 |
| 对话引导类 | 提问 / 访谈 / 澄清 / 引导 / 选项 / 确认 |
| 工具辅助类 | 评估 / 诊断 / 检查 / 脚本 / lint / 测试 |
| 通用类 | 以上都不命中 |

## 四、各类别清单

### 知识库编译类（如 second-brain）

| # | 该有 | 核对结论 |
|:--:|:---|:---|
| 1 | 去重机制（hash/标识，防重复摄入）| ☐ |
| 2 | 数据合法性校验（JSON/结构，损坏不阻塞或显式处理）| ☐ |
| 3 | 索引完整性（索引存在且可校验无悬空/未收录）| ☐ |
| 4 | 缓存大小管理（超限截断/清理策略）| ☐ |
| 5 | 队列/待处理机制（延迟任务有存储+回填路径）| ☐ |
| 6 | 文件操作安全（读回验证/原子写/防覆盖）| ☐ |
| 7 | 可机器化操作已脚本化（对照上 6 项逐项核对）| ☐ |

### 工作流自动化类（如 loop-engineering / clock-loop）

| # | 该有 | 核对结论 |
|:--:|:---|:---|
| 1 | 阶段门禁（每阶段产出物校验，不过不进下一步）| ☐ |
| 2 | 熔断器（连败/超时/卡死，止损不空转）| ☐ |
| 3 | 进度持久化（跨会话/中断不丢）| ☐ |
| 4 | 验收闭环（明卷+暗卷，执行者不能自批）| ☐ |
| 5 | 幂等性（重复跑不产生副作用）| ☐ |
| 6 | 可机器化操作已脚本化 | ☐ |

### 对话引导类（如 leader-translator）

| # | 该有 | 核对结论 |
|:--:|:---|:---|
| 1 | 提问轮次上限（不无限追问）| ☐ |
| 2 | 确认机制（逐轮/逐项，误解可纠正）| ☐ |
| 3 | 兜底（用户沉默/说不清/不在场有默认路径）| ☐ |
| 4 | 产出物锁（每环节有可验产出，不空谈）| ☐ |
| 5 | 边界（不该接的场景显式排除）| ☐ |
| 6 | 可机器化操作已脚本化 | ☐ |

### 工具辅助类（如 skill-rehab 自身）

| # | 该有 | 核对结论 |
|:--:|:---|:---|
| 1 | 自检（--self 类，本体可自证）| ☐ |
| 2 | 回归测试（goldens/测试集，升级防越改越瞎）| ☐ |
| 3 | 版本管理（snapshot/verify，改动留痕）| ☐ |
| 4 | 失败模式防御（常见翻车点有对策）| ☐ |
| 5 | 可机器化操作已脚本化 | ☐ |

### 通用类（兜底）

| # | 该有 | 核对结论 |
|:--:|:---|:---|
| 1 | 触发/边界（该触发触发、不该触发排除）| ☐ |
| 2 | 分步（≥3 连续步骤，每步有产出）| ☐ |
| 3 | 验收（可执行判定，不只「看看效果」）| ☐ |
| 4 | 失败处理（出错有止损，不静默）| ☐ |
| 5 | 可机器化操作已脚本化 | ☐ |

## 五、复查闭环

- 治疗完成后：**重跑评估器**（分 ≥ 修复前，未过项清零）+ **本清单复查**（每项从 ☐ 变为 脚本化✓/命令化✓/显式留AI✓，缺口清零）
- **两源都过才算达标**——评估器满分 ≠ 完整性过；完整性过 ≠ 评估器满分。缺任一源，如实报告，不许说「做完了」。
